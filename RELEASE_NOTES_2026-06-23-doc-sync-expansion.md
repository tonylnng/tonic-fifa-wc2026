# Release Notes — 文檔自動同步範圍擴充（2026-06-23 HKT）

## 摘要

擴充 `push_to_github.py` 的關鍵說明文檔自動同步範圍，並全面掃描整個專案文檔結構，補回所有「只存在於 repo、工作區缺少權威來源」的孤兒文檔，順手修正其中過時的執行頻率描述。

## 變更內容

### 1. 自動同步清單擴充至 7 份核心說明文檔

`push_to_github.py` 的 `DOC_SYNC_LIST` 現包含（相對專案根目錄，支援子資料夾）：

| # | 文檔 | 說明 |
|---|---|---|
| 1 | `CRON_RUNBOOK.md` | 排程每輪執行的完整手冊 |
| 2 | `PREDICTION_METHODOLOGY.md` | 預測方法論 |
| 3 | `README.md` | 專案總覽 |
| 4 | `部署說明.md` | 部署說明 |
| 5 | `automation/排程自動化總覽.md` | 自動化邏輯與檔案總覽 |
| 6 | `deploy/自動部署說明.md` | 伺服器自動部署 |
| 7 | `docs/技術圖表.md` | 技術圖表說明 |

### 2. 同步邏輯升級

- 改為支援**子資料夾相對路徑**，並自動建立目的地子目錄（`os.makedirs(..., exist_ok=True)`）。
- 來源不存在的項目會被跨過，**不會覆蓋 repo 現有版本**。

### 3. 補回孤兒文檔 + 修正過時頻率

掃描發現 #4–#7 共 4 份文檔原本只存在於 repo、工作區根目錄沒有權威來源（與先前 README 相同），已從 repo 複製回工作區作為日後同步來源。同時修正其中 5 處過時的執行頻率描述：

- `部署說明.md`：建議排程「每 8 小時／16 強起每 4 小時」→ **每日一次（HKT 13:00）**。
- `automation/排程自動化總覽.md`：3 處「每 8 小時／16 強起每 4 小時」→ **每日一次（UTC `0 5 * * *` = HKT 13:00）**。
- `deploy/自動部署說明.md`：輪詢說明的頻率描述 → **每日一次（HKT 13:00）**。

## 影響範圍

- repo：`push_to_github.py`、上述 7 份說明文檔。
- 未來每次定時任務推送都會自動把這 7 份核心說明文檔同步到 repo 作參考。
