# Release Notes — 成本優化 + Runbook 同步至 repo（2026-06-23 HKT）

## 摘要

本次為**流程／成本優化與文檔同步**更新（非預測資料批次）。應使用者要求降低每日自動預測流程的整體成本，並把執行手冊 `CRON_RUNBOOK.md` 正式納入 repo 作為參考、且讓未來每次更新都自動同步。

## 變更內容

### 1. 成本優化（方案 A + D + E）

| 方案 | 項目 | 由 | 改為 |
|---|---|---|---|
| **A** | 來源數門檻 | ≥100（≤36h）/ ≥50（48h 窗口） | **≥40 / ≥25** |
| **D** | 執行頻率 | 每日兩次 07:00 / 19:00 HKT（`0 11,23 * * *`） | **每日一次 13:00 HKT（`0 5 * * *`）** |
| **E** | 研究子代理 | 每場各起一個 | **一個子代理一次研究多場**（>3 場分 2 組） |
| — | 第三方對照 AI | — | **維持 7 家不變**（MiniMax/Qwen/DeepSeek/GPT/Gemini/Grok/GLM） |

- 淘汰賽（32 強起）同樣維持每日一次，不再切換為每日兩次或每 4 小時。
- 主預測模型仍為 Claude Sonnet 4.6，命中率（目前 59.5%）預期基本維持。

### 2. Runbook 納入 repo 並自動同步

- `CRON_RUNBOOK.md` 更新為成本優化版（排程、來源門檻、合併子代理段落）。
- `push_to_github.py` 新增「同步根目錄參考文檔」區塊：每次推送會自動把專案根目錄的 `CRON_RUNBOOK.md` 複製到 repo 根目錄，**確保未來每次更新都同步一份到 repo 作參考**。

## 影響範圍

- 定時任務 `c53acfd7`：排程改 `0 5 * * *`，名稱改為「每日一次 13:00 HKT・成本優化版」。
- 技能 `wc2026-prediction-automation`：Standing Rules、Pipeline 摘要、`references/runbook.md` 同步更新。
- repo：`CRON_RUNBOOK.md`、`push_to_github.py` 更新。

## 預估效果

頻率減半（D）＋ 研究量大幅下降（A）＋ 子代理固定開銷下降（E），每日總成本預估降至原本約 **1/3～2/5**。
